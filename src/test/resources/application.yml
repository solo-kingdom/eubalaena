# encrypt: -Dkey-file=/path/to/key.properties

spring:
  application:
    name: eubalaena
  datasource:
    url: jdbc:mysql://localhost:3306/eubalaena?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    username: test
    password: test
  redis:
    port: 6379
    host: 127.0.0.1

auth:
  enable: true
  type: token

eubalaena:
  file:
    location: /tmp/test